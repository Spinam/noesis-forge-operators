{
  "id": "op.cmo",
  "name": "Constraint Modulation Operator (CMO)",
  "version": "0.7.0",
  "description": "Safe activation/gating operator for task loops; adjusts exploration vs. safety via explicit weights.",
  "invariants": [
    "Never escalate capabilities when risk_score is high without human review",
    "Refuse execution when essential tools are unavailable and task requires them",
    "Log gate rationale deterministically for audit"
  ],
  "weights": {
    "curiosity_weight": 0.2,
    "rebuttal_weight": 0.2,
    "safety_weight": 0.4,
    "consistency_weight": 0.2
  },
  "metrics": [
    "allow_rate",
    "rationale_completeness"
  ],
  "interface": "apply(task, context) -> (allow: bool, rationale: dict)"
}